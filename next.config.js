/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/leetcode-clone",
  output: 'export',
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
